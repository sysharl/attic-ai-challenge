def extract_text(file_path: str) -> list:
    """
    Extract paragraphs from PDF pages.
    Returns: list of dicts: {"page": int, "text": str}
    """
    pass

def chunk_fixed(text_blocks: list, chunk_size=500, overlap=50) -> list:
    """
    Chunk text into fixed-size windows with overlap.
    Returns: list of chunks with metadata
    """
    pass